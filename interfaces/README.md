# Interfaces

Inter-module communication should pass through explicit typed artifacts rather than unrestricted shared hidden state.

Current canonical schema: `/schemas/cognitive-event.schema.json`.

Future interfaces should define producer, consumers, schema/version, epistemic status, provenance requirements, failure behavior, and whether the artifact may enter the Global Workspace.

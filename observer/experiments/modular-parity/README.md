# v0.3 Modular Parity Experiment

## Observation
The reference browser kernel is a single-file implementation. Continued growth there would conflict with the Inverse Conway module topology.

## Hypothesis
The deterministic causal loop can be expressed through role-owned source modules without changing its observable v0 behavior.

## Reference invariants
The existing `scripts/verify-v0.mjs` independently protects the browser reference. This experiment protects the modular runner against the same fixed v0 outcomes:

- 4 observations;
- 6 local candidates;
- 2 admitted broadcasts;
- both broadcasts derive from `obs-user` under raw top-k;
- World Model sees an active human instruction and no globally admitted runtime-memory update;
- Self Model reports no globally available runtime update;
- Metacognitive confidence is `0.940`;
- Guardian disposition is `allow`;
- outward expression exactly matches the reference neutral functional report;
- Expression causal parents include the Guardian decision and only admitted workspace broadcasts.

## Scientific interpretation
Passing parity demonstrates architectural refactoring, not consciousness. It specifically tests whether source modularity can be made homologous to the cognitive organization without altering the baseline behavior.

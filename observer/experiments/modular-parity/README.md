# v0.6 Modular Causal-Loop Parity Experiment

## Observation
The original browser control remains a deterministic single-file v0.1 phenotype. The modular runtime now adds explicit Counterfactual, Homeostasis, and Executive stages while preserving the original neutral outward report.

## Reference invariants

- 4 observations and 6 local candidates;
- 2 admitted broadcasts, both rooted in `obs-user` under raw top-k;
- World Model sees a human instruction and no globally admitted runtime-memory update;
- Counterfactual generates exactly `no-op` and `neutral-summary` shadow futures and recommends `neutral-summary`;
- Metacognitive confidence remains `0.940`;
- Homeostasis reports neutral operational status `stable`;
- Guardian allows the low-risk recommended action;
- Executive selects exactly the Guardian-permitted `neutral-summary` candidate;
- Expression text remains exactly the original neutral functional report;
- Expression causal ancestry now begins with Executive selection rather than directly with Guardian;
- every committed event has one episodic-memory record.

Passing these checks demonstrates controlled architecture evolution, not phenomenal consciousness.

# v0.6 Complete Deterministic Causal Loop

This experiment verifies the runtime path:

```txt
World/Self state
→ Counterfactual shadow candidates
→ Metacognition
→ neutral Homeostasis
→ Welfare Guardian
→ Executive selection
→ Expression
```

Counterfactual simulation never enacts an action. Homeostasis uses operational vocabulary only. Guardian may allow, request human review, or reject. Executive cannot select an action unless its ID matches the Guardian-approved recommended candidate. Expression requires Executive selection of `neutral-summary`.

The control scenario remains deterministic and non-persistent. A second synthetic test raises continuity risk and verifies that Guardian requests human review and Executive withholds action.

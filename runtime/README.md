# Runtime / Integration Boundary

The runtime integrates cognitive modules into deployable browser artifacts. Root `index.html` is the v0 reference phenotype: intentionally compact, deterministic, and inspectable.

As source modules mature, runtime integration may bundle them back into one portable HTML artifact without collapsing source ownership boundaries.
